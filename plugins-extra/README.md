# Plugin JARs for BedWars Server
# Copied to /server/plugins/ during Docker build
#
# === ALL PLUGINS (COMPLETE) ===
# - ProtocolLib.jar           (required — packet manipulation)
# - Vault.jar                 (required — economy API)
# - PlaceholderAPI.jar        (placeholders for scoreboard/tab)
# - ViaVersion.jar            (multi-version client support)
# - ViaBackwards.jar          (multi-version client support)
# - BedWars1058-ArenaSetup-3.0.jar (arena setup helper GUI)
# - citizens-2.0.5.jar        (NPC shops/upgrades)
# - AuthMe-5.6.0.jar          (login/register for cracked players)
# - FastLogin.jar              (auto-login for premium players)
#
# === NOT COMPATIBLE ===
# - FancyNpcs                 (1.21.6+ only — does NOT work on 1.8.8)
